#include <stdio.h>
#include "codes.h"

#if 0
#include <stdbool.h>
#include <ctype.h>
#include <string.h>
#include <stdlib.h>
#endif

int find_code(const struct dialing_code country_codes[]);

int main(void)
{
    int code, index = 0;

    printf("Enter country code (0 - exit): ");
    scanf("%d", &code);


    return 0;
}
